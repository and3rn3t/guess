import { ComingSoon } from '../ComingSoon'

export default function PipelineRoute(): React.JSX.Element {
  return <ComingSoon title="Pipeline Audit Log" description="Step-by-step enrichment provenance trail per character. Planned: AD.5." />
}
