import { ComingSoon } from '../ComingSoon'

export default function QuestionsRoute(): React.JSX.Element {
  return <ComingSoon title="Question Manager" description="Paginated question list with usage stats, inline editing, and disable/enable toggle. Planned: AD.7." />
}
