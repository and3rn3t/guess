import { ComingSoon } from '../ComingSoon'

export default function CharactersRoute(): React.JSX.Element {
  return <ComingSoon title="Character Manager" description="Searchable/filterable table of all characters with inline attribute editing. Planned: AD.6." />
}
