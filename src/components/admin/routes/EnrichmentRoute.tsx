import { ComingSoon } from '../ComingSoon'

export default function EnrichmentRoute(): React.JSX.Element {
  return <ComingSoon title="Enrichment Status" description="Character enrichment progress: totals, failures, and retry controls. Planned: AD.4." />
}
